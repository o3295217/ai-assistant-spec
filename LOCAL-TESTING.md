# 🧪 Локальное тестирование

## ⚡ Быстрый старт

```bash
# 1. Убедитесь что .env настроен
cat .env

# 2. Проверьте базу данных
ls -lh prisma/dev.db

# 3. Запустите приложение
npm run dev

# 4. Откройте браузер
# http://localhost:3000
```

---

## 📋 Полная настройка (если база пустая)

### 1. Установка зависимостей

```bash
npm install
# ИЛИ с игнорированием ошибок Prisma CDN
PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 npm install
```

### 2. Настройка .env

```bash
# Убедитесь что .env существует
cp .env.example .env

# Отредактируйте .env
DATABASE_URL="file:./dev.db"
ANTHROPIC_API_KEY="sk-ant-ваш-реальный-ключ"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

⚠️ **ВАЖНО:** Замените `sk-ant-ваш-реальный-ключ` на настоящий API ключ от Anthropic!

### 3. Создание базы данных

```bash
# Применить миграции (создает все таблицы)
npx prisma migrate dev

# Вставить начальные данные (14 критериев)
npx prisma db seed
# ИЛИ
npm run seed
```

### 4. Проверка базы данных

```bash
# Визуальный просмотр БД
npx prisma studio
# Откроется http://localhost:5555

# ИЛИ через скрипт
node check-database.js
```

### 5. Запуск приложения

```bash
# Development режим с hot reload
npm run dev

# ИЛИ production build
npm run build
npm start
```

---

## 🔍 Проверка работоспособности

### 1. Главная страница
- http://localhost:3000
- Должна показать дашборд

### 2. Ежедневное планирование
- http://localhost:3000/daily
- Проверьте что есть поле "Контекст дня / Окружающий мир" ✅

### 3. Критерии оценки
- http://localhost:3000/profile/criteria
- Должно быть 14 критериев по категориям

### 4. Профиль
- http://localhost:3000/profile
- Можно заполнить информацию о себе

---

## 🐛 Решение проблем

### Ошибка: "Cannot find module '@prisma/client'"

```bash
# Регенерировать клиент
PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 npx prisma generate

# ИЛИ если не работает - удалить и переустановить
rm -rf node_modules/.prisma
npm install
```

### Ошибка: "Table not found"

```bash
# База не создана - применить миграции
npx prisma migrate dev

# Вставить начальные данные
npx prisma db seed
```

### Ошибка: "API key not valid"

```bash
# Проверить .env
cat .env | grep ANTHROPIC_API_KEY

# Убедитесь что ключ настоящий, начинается с sk-ant-
```

### Ошибка: "No criteria selected"

```bash
# Вставить критерии
npx prisma db seed

# Проверить
node check-database.js
```

### База данных сломалась

```bash
# ВНИМАНИЕ: Удалит все данные!
rm prisma/dev.db

# Пересоздать с нуля
npx prisma migrate dev
npx prisma db seed
```

---

## 📊 Полезные команды

```bash
# Просмотр БД в браузере
npx prisma studio

# Проверка схемы
npx prisma validate

# Форматирование схемы
npx prisma format

# Сброс БД (удалит данные!)
npx prisma migrate reset

# Просмотр миграций
ls prisma/migrations/

# Логи Next.js
npm run dev -- --debug
```

---

## 🎯 Тестовый сценарий

### 1. Создать цели

1. Перейти в http://localhost:3000/goals
2. Добавить "Мечта" (5 лет)
3. Добавить цели на год, полугодие, квартал, месяц, неделю

### 2. Заполнить профиль

1. http://localhost:3000/profile
2. Указать имя, должность, возраст и т.д.

### 3. Выбрать критерии

1. http://localhost:3000/profile/criteria
2. Выбрать критерии для оценки (например: Стратегия, Семья, Здоровье)

### 4. Запланировать день

1. http://localhost:3000/daily
2. Написать план на день
3. В конце дня - написать факт
4. ✨ **НОВОЕ:** Заполнить "Контекст дня" (наблюдения, события, мысли)

### 5. Получить оценку

1. Нажать "Получить оценку от ИИ"
2. Подождать 5-10 секунд
3. Увидеть оценки по выбранным критериям + фидбек

### 6. Посмотреть аналитику

1. http://localhost:3000/analytics
2. Графики по всем критериям
3. Динамика общей оценки

---

## 🔐 Безопасность

✅ `.env` в `.gitignore` - секреты не попадают в Git
✅ База данных `*.db` тоже не попадает в Git
✅ API ключ только локально

**Никогда не коммитьте:**
- `.env` с реальным API ключом
- `prisma/dev.db` с данными

---

## 📝 Структура проекта

```
ai-assistant-spec/
├── app/                    # Next.js страницы и API
│   ├── api/               # Backend API routes
│   │   ├── daily/         # Ежедневные записи
│   │   ├── evaluate/      # Оценка от Claude
│   │   ├── criteria/      # Управление критериями ✨ NEW
│   │   ├── goals/         # Цели (dream, period)
│   │   └── profile/       # Профиль пользователя
│   ├── daily/             # Страница планирования
│   ├── evaluation/        # Результаты оценки
│   ├── goals/             # Управление целями
│   ├── profile/           # Профиль и настройки
│   │   └── criteria/      # Выбор критериев ✨ NEW
│   └── analytics/         # Графики и статистика
├── lib/                   # Утилиты
│   ├── anthropic.ts       # Claude API (с кэшированием ✨)
│   ├── prisma.ts          # Prisma клиент
│   └── dates.ts           # Работа с датами
├── prisma/                # База данных
│   ├── schema.prisma      # Схема БД (обновлена ✨)
│   ├── migrations/        # История изменений
│   ├── dev.db            # SQLite файл (локально)
│   └── seed.js           # Начальные данные ✨ NEW
├── .env                   # Секреты (не в Git!)
└── package.json          # Зависимости

✨ = Новое в этой версии
```

---

## 🚀 Готово к локальному тестированию!

После запуска `npm run dev`:
- ✅ Приложение доступно на http://localhost:3000
- ✅ Поле "Контекст дня" работает
- ✅ Кастомные критерии оценки
- ✅ Холистический анализ от Claude AI
- ✅ Кэширование промптов (экономия 90%)

Удачного тестирования! 🎉
