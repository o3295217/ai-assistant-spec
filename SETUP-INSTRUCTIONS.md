# 🚀 Инструкция по настройке и запуску проекта

**Путь к вашему локальному проекту:** `/Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec`

---

## 📋 Оглавление

1. [Первая настройка](#первая-настройка)
2. [Автоматическая синхронизация с GitHub](#автоматическая-синхронизация-с-github)
3. [Запуск в Docker](#запуск-в-docker)
4. [Ежедневная работа](#ежедневная-работа)

---

## Первая настройка

### Шаг 1: Откройте Terminal на вашем Mac

```bash
# Перейдите в родительскую директорию
cd /Users/oleggluskov/Documents/GooglDisk
```

### Шаг 2: Склонируйте проект (если еще не сделано)

```bash
# Если папка ai-assistant-spec уже существует, пропустите этот шаг
git clone https://github.com/o3295217/ai-assistant-spec.git
cd ai-assistant-spec
git checkout claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw
```

### Шаг 3: Или обновите существующий проект

```bash
# Если папка ai-assistant-spec уже существует
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
git fetch origin
git checkout claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw
git pull origin claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw
```

### Шаг 4: Настройте Anthropic API ключ

```bash
# Откройте файл .env.local
nano .env.local

# Замените:
# ANTHROPIC_API_KEY="sk-ant-your-api-key-here"
# на ваш настоящий ключ от https://console.anthropic.com/

# Сохраните: Ctrl+O, Enter, Ctrl+X
```

### Шаг 5: Убедитесь, что Docker Desktop установлен и запущен

- Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
- Запустите Docker Desktop
- Дождитесь, пока в трее появится зеленый индикатор

---

## Автоматическая синхронизация с GitHub

### 🔄 Когда я (Claude) вношу изменения в проект:

Все изменения автоматически коммитятся и пушатся в GitHub на ветку:
**`claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw`**

### 📥 Чтобы получить последние изменения на ваш Mac:

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
git pull origin claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw
```

### 🤖 Автоматический скрипт синхронизации

Я создал скрипт `sync-to-github.sh`, который автоматически:
- Проверяет изменения
- Коммитит их
- Пушит на GitHub

**Вы можете использовать его, если вносите локальные изменения:**

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./sync-to-github.sh
# Или с кастомным сообщением:
./sync-to-github.sh "Добавил новую функцию"
```

---

## Запуск в Docker

### 🚀 Метод 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

Я создал скрипт `deploy-local.sh`, который делает ВСЁ автоматически:

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./deploy-local.sh
```

Этот скрипт:
- ✅ Проверит, что проект существует (или склонирует его)
- ✅ Получит последние изменения с GitHub
- ✅ Проверит Docker
- ✅ Создаст базу данных (если нужно)
- ✅ Соберет Docker образ
- ✅ Запустит контейнер
- ✅ Откроет http://localhost:3000 в браузере

**Это всё, что вам нужно!**

### 🔧 Метод 2: Пошагово вручную

Если хотите больше контроля:

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec

# 1. Получить последние изменения
git pull origin claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw

# 2. Создать базу данных (первый раз)
npm install better-sqlite3 --no-save
node setup-db.js

# 3. Запустить Docker
./docker-start.sh

# 4. Открыть в браузере
open http://localhost:3000
```

### 🛑 Остановка контейнера

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./docker-stop.sh

# Или:
docker-compose down
```

### 📋 Просмотр логов

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./docker-logs.sh

# Или:
docker-compose logs -f
```

---

## Ежедневная работа

### 📅 Типичный рабочий день:

#### Утро (запуск):
```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./deploy-local.sh
# Откройте http://localhost:3000
```

#### День (работа с приложением):
- Планируйте задачи на день
- Вносите факты выполнения
- Получайте AI оценку от Claude
- Смотрите аналитику и alignment

#### Вечер (остановка):
```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./docker-stop.sh
```

### 🔄 Когда я (Claude) обновил код:

1. **Вы получите уведомление** о новых изменениях
2. **Остановите контейнер:**
   ```bash
   ./docker-stop.sh
   ```
3. **Запустите снова** (скрипт автоматически подтянет изменения):
   ```bash
   ./deploy-local.sh
   ```

### 📊 Если нужно посмотреть последние изменения:

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
git log --oneline -10  # Последние 10 коммитов
git show              # Последний коммит подробно
```

---

## 🎯 Быстрые команды (шпаргалка)

```bash
# Перейти в проект
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec

# Получить последние изменения с GitHub
git pull origin claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw

# Запустить приложение в Docker (всё автоматически)
./deploy-local.sh

# Запустить Docker (быстрый старт)
./docker-start.sh

# Остановить Docker
./docker-stop.sh

# Посмотреть логи
./docker-logs.sh

# Отправить свои изменения на GitHub
./sync-to-github.sh "Описание изменений"

# Проверить статус Docker контейнера
docker ps

# Открыть приложение в браузере
open http://localhost:3000
```

---

## 📚 Дополнительная документация

- **DOCKER.md** - Полное руководство по Docker deployment
- **QUICKSTART-MAC.md** - Быстрый старт для macOS
- **LOGGING.md** - Система логирования
- **README.md** - Общая документация проекта
- **ai-assistant-spec.md** - Техническое задание

---

## ❓ Troubleshooting

### Проблема: "git pull" требует merge

```bash
# Сбросить локальные изменения (ОСТОРОЖНО!)
git reset --hard origin/claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw

# Или сохранить локальные изменения:
git stash
git pull origin claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw
git stash pop
```

### Проблема: Docker контейнер не запускается

```bash
# Проверить логи
docker-compose logs

# Полностью пересобрать
./docker-stop.sh
docker-compose build --no-cache
./docker-start.sh
```

### Проблема: Порт 3000 занят

```bash
# Найти процесс
lsof -i :3000

# Убить процесс (замените PID на реальный)
kill -9 PID
```

### Проблема: База данных повреждена

```bash
# Создать backup
cp prisma/dev.db prisma/dev.db.backup

# Пересоздать базу
rm prisma/dev.db
npm install better-sqlite3 --no-save
node setup-db.js
```

---

## 🎉 Всё готово!

Теперь у вас есть полностью автоматизированный workflow:

1. **Я (Claude)** вношу изменения → автоматически пушу на GitHub
2. **Вы** запускаете `./deploy-local.sh` → получаете последнюю версию в Docker
3. **Приложение** доступно на http://localhost:3000

**Приятной работы!** 🚀
