# 🚀 Деплой и Использование

## 1. 📦 Скрипты для быстрого коммита (Mac)

### Вариант 1: С запросом сообщения (`quick-commit.command`)
Двойной клик → введите сообщение → автоматический коммит и пуш

### Вариант 2: Автоматический (`auto-commit.command`)
Двойной клик → мгновенный коммит с timestamp и пуш

**Использование:**
1. Просто дважды кликните на файл `.command` в Finder
2. При первом запуске macOS может попросить разрешение
3. Готово!

---

## 2. 🌐 Деплой приложения на Vercel (из GitHub)

### ✅ Преимущества Vercel для Next.js:
- **Бесплатно** для личных проектов
- **Автоматический деплой** при каждом push в GitHub
- **HTTPS** из коробки
- **Переменные окружения** защищены
- Создан командой Next.js

### 🔒 Безопасность API ключей:

**НЕ ВОЛНУЙТЕСЬ!** Ваш `.env` файл **НЕ попадает** в GitHub:
- ✅ `.env` уже в `.gitignore` (строка 27)
- ✅ База данных (`*.db`) тоже в `.gitignore`
- ✅ Секреты хранятся **только локально**

### 📝 Как задеплоить:

#### Шаг 1: Регистрация на Vercel
```bash
# Установить Vercel CLI (опционально)
npm i -g vercel
```
**ИЛИ** просто зайти на https://vercel.com

#### Шаг 2: Подключить GitHub репозиторий
1. Зайти на https://vercel.com/new
2. Выбрать репозиторий `o3295217/ai-assistant-spec`
3. Vercel автоматически определит Next.js

#### Шаг 3: Настроить переменные окружения
В настройках проекта Vercel → Environment Variables:
```
ANTHROPIC_API_KEY = sk-ant-ваш-настоящий-ключ
DATABASE_URL = file:./data/prod.db
NEXT_PUBLIC_APP_URL = https://your-app.vercel.app
```

#### Шаг 4: Deploy!
Нажать **Deploy** → готово через 30 секунд

---

## 3. 🗄️ База данных в продакшене

**Проблема:** SQLite файл не сохраняется между деплоями на Vercel (serverless).

**Решения:**

### Вариант A: Vercel Postgres (рекомендуется)
```bash
# Создать БД в Vercel
# Dashboard → Storage → Create → Postgres

# Обновить DATABASE_URL в Environment Variables
DATABASE_URL="postgresql://..."
```

### Вариант B: Turso (бесплатно, SQLite в облаке)
```bash
npm install @libsql/client
# https://turso.tech
```

### Вариант C: PlanetScale / Supabase
- PlanetScale: MySQL-совместимый
- Supabase: PostgreSQL с бесплатным тиром

---

## 4. 🔐 Безопасность (чеклист)

✅ `.env` в `.gitignore` - **ДА**
✅ `.env.example` без реальных ключей - **ДА**
✅ База данных не в Git - **ДА**
✅ API ключи только в переменных окружения - **ДА**

**Никогда не коммитьте:**
- `.env` файлы с реальными ключами
- `*.db` файлы с данными
- Credentials, пароли, токены

---

## 5. 🎯 Готовая инструкция деплоя

### На Vercel (проще всего):
```bash
# 1. Установить Vercel CLI
npm i -g vercel

# 2. Залогиниться
vercel login

# 3. Задеплоить из корня проекта
vercel

# 4. Добавить переменные окружения
vercel env add ANTHROPIC_API_KEY
# Ввести ваш настоящий ключ

# 5. Готово!
vercel --prod
```

### Автоматический деплой через GitHub:
1. Push в `main` → автоматический деплой
2. Push в другие ветки → preview деплой (для тестов)
3. Pull Request → автоматический preview

---

## 6. 📊 Мониторинг

После деплоя доступно:
- **Логи** - реальном времени в Vercel Dashboard
- **Аналитика** - посещаемость, производительность
- **Error tracking** - автоматический мониторинг ошибок

---

## 🆘 Частые проблемы

**Q: База данных пустая после деплоя?**
A: SQLite-файл не переносится. Нужна облачная БД (Vercel Postgres).

**Q: Ошибка "API key not found"?**
A: Добавить `ANTHROPIC_API_KEY` в Environment Variables на Vercel.

**Q: Миграции не применились?**
A: Добавить в `package.json`:
```json
"scripts": {
  "build": "prisma generate && prisma migrate deploy && next build"
}
```

---

## 🎉 Готово!

Теперь ваше приложение:
- ✅ Доступно по HTTPS
- ✅ Автоматически деплоится при push
- ✅ Безопасно (секреты защищены)
- ✅ Бесплатно (до определенных лимитов)
