# 🍎 Настройка скриптов для Mac

## 📍 Где находится проект на вашем Mac?

Обычно проекты клонируются в:
- `~/Projects/ai-assistant-spec`
- `~/Developer/ai-assistant-spec`
- `~/Documents/GitHub/ai-assistant-spec`
- `~/Desktop/ai-assistant-spec`

**Узнать путь:**
```bash
# Откройте Terminal и перейдите в папку проекта
cd путь/к/проекту

# Узнайте полный путь
pwd
# Например: /Users/username/Projects/ai-assistant-spec
```

---

## 🚀 Скрипты для коммита (3 варианта)

### 1️⃣ `commit-all.command` - Полный контроль
**Что делает:**
- Показывает все изменения
- Запрашивает сообщение коммита
- Коммитит и пушит в GitHub
- Повторяет при ошибках сети (до 4 попыток)

**Использование:** Двойной клик в Finder

---

### 2️⃣ `quick-push.command` - Быстрый пуш
**Что делает:**
- Показывает изменения
- Автосообщение: "Update: 2024-11-20 15:30"
- Одно нажатие Enter → готово!

**Использование:** Двойной клик в Finder

---

### 3️⃣ `auto-commit.command` - Мгновенный
**Что делает:**
- Двойной клик → мгновенный коммит и пуш
- Автосообщение с timestamp
- Без вопросов!

**Использование:** Двойной клик в Finder

---

### 4️⃣ `quick-commit.command` - Оригинальный
**Что делает:**
- Запрашивает сообщение
- Коммитит и пушит

**Использование:** Двойной клик в Finder

---

## ⚙️ Настройка (один раз)

### Шаг 1: Клонировать репозиторий (если еще не сделано)

```bash
# Откройте Terminal (Cmd + Space → Terminal)
cd ~/Projects  # или куда хотите

# Клонируйте репозиторий
git clone https://github.com/o3295217/ai-assistant-spec.git
cd ai-assistant-spec
```

### Шаг 2: Дать права на выполнение

```bash
# В папке проекта выполните:
chmod +x *.command
```

**ИЛИ через Finder:**
1. Правой кнопкой на файл `.command`
2. "Свойства" → "Права"
3. Поставить галочку "Исполняемый файл"

### Шаг 3: Первый запуск (разрешение macOS)

При первом двойном клике:
1. macOS скажет: "Не удается открыть, так как файл от неизвестного разработчика"
2. Откройте **Системные настройки** → **Конфиденциальность и безопасность**
3. Внизу будет кнопка **"Все равно открыть"**
4. Подтвердите

**ИЛИ сразу разрешить через Terminal:**
```bash
# Для всех .command файлов
xattr -cr *.command
```

### Шаг 4: Готово! 🎉

Теперь просто **дважды кликайте** на `.command` файлы в Finder!

---

## 🎯 Какой скрипт выбрать?

### Для ежедневной работы:
→ **`quick-push.command`** - баланс скорости и контроля

### Когда нужен контроль:
→ **`commit-all.command`** - видите все изменения, пишете сообщение

### Максимальная скорость:
→ **`auto-commit.command`** - один клик, ничего не спрашивает

### Первый раз тестируете:
→ **`quick-commit.command`** - простой и понятный

---

## 🔧 Продвинутая настройка

### Создать алиас в Terminal

Добавьте в `~/.zshrc` (или `~/.bash_profile`):

```bash
# Быстрый коммит из Terminal
alias push='cd ~/Projects/ai-assistant-spec && git add -A && git commit -m "Quick update" && git push'

# Коммит с сообщением
gcp() {
    cd ~/Projects/ai-assistant-spec
    git add -A
    git commit -m "$1"
    git push
}

# Использование:
# gcp "Добавил новую функцию"
```

### Добавить в Dock

1. Перетащите `.command` файл в Dock
2. Теперь один клик из Dock!

### Создать хоткей (через Automator)

1. Откройте **Automator**
2. Создайте **Quick Action**
3. Добавьте **Run Shell Script**
4. Вставьте содержимое скрипта
5. Сохраните как "Git Push"
6. **Системные настройки** → **Клавиатура** → **Shortcuts** → назначьте хоткей (например Cmd+Shift+P)

---

## 🆘 Решение проблем

### Скрипт не запускается (двойной клик не работает)

```bash
# Проверьте права
ls -la *.command

# Должно быть: -rwxr-xr-x (x = исполняемый)
# Если нет, дайте права:
chmod +x *.command
```

### Ошибка: "Не git репозиторий"

```bash
# Убедитесь что вы в правильной папке
cd ~/Projects/ai-assistant-spec  # путь к вашему проекту

# Проверьте что это git репозиторий
git status
```

### Ошибка: "Permission denied"

```bash
# Проверьте SSH ключи для GitHub
ssh -T git@github.com

# Если ошибка, настройте SSH:
# https://docs.github.com/en/authentication/connecting-to-github-with-ssh
```

### Не находит команду git

```bash
# Установите Xcode Command Line Tools
xcode-select --install
```

---

## 📂 Структура скриптов в проекте

```
ai-assistant-spec/
├── commit-all.command      ← Полный контроль ⭐
├── quick-push.command      ← Быстрый пуш (рекомендуется) 🚀
├── auto-commit.command     ← Мгновенный ⚡
└── quick-commit.command    ← Простой 📝
```

---

## 🎓 Как это работает

### `.command` расширение
- macOS автоматически открывает `.command` файлы в Terminal
- Двойной клик = запуск скрипта
- Не нужно открывать Terminal вручную!

### git add -A
- Добавляет **все** изменения (новые, измененные, удаленные файлы)

### git push origin <branch>
- Отправляет в текущую ветку
- Автоматически определяет ветку
- Ретраи при ошибках сети

---

## 💡 Советы

1. **Не забывайте .env!**
   - `.env` уже в `.gitignore`
   - Ваши секреты в безопасности ✅

2. **Проверяйте изменения**
   - Используйте `commit-all.command` для важных изменений
   - Смотрите что коммитите!

3. **Автоматизируйте рутину**
   - `quick-push.command` для частых мелких изменений
   - Сохраняет время!

4. **Используйте осмысленные сообщения**
   - Для важных коммитов пишите понятные сообщения
   - Будущий вы скажет спасибо 😊

---

## ✅ Готово!

Теперь коммит изменений = **двойной клик в Finder** 🎉

**Не помните где проект?** Найдите в Spotlight (Cmd + Space → "ai-assistant-spec")
