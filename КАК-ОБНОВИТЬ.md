# 🔄 Как обновить приложение

Когда я (Claude) вношу изменения в проект, вам нужно обновить локальную версию.

---

## 🖱️ Способ 1: Двойной клик (САМЫЙ ПРОСТОЙ)

1. **Откройте Finder**
2. **Перейдите в папку проекта:**
   ```
   /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
   ```
3. **Найдите файл:** `update-and-restart.command`
4. **Сделайте двойной клик** по файлу

Скрипт автоматически:
- ✅ Получит последние изменения с GitHub
- ✅ Пересоберёт Docker образ
- ✅ Перезапустит контейнер
- ✅ Откроет приложение в браузере (если подтвердите)

---

## ⌨️ Способ 2: Через Terminal

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./update-and-restart.command
```

Или выполните команды вручную:

```bash
# 1. Получить обновления
git pull origin claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw

# 2. Пересобрать и перезапустить Docker
docker-compose down
docker-compose build
docker-compose up -d

# 3. Открыть в браузере
open http://localhost:3000
```

---

## 🚀 Способ 3: Полный автоматический деплой

```bash
cd /Users/oleggluskov/Documents/GooglDisk/ai-assistant-spec
./deploy-local.sh
```

Этот скрипт делает всё то же самое, что и `update-and-restart.command`.

---

## ⚡ Быстрый перезапуск (без обновления)

Если изменения не нужны, просто перезапустить контейнер:

```bash
docker-compose restart
```

---

## 📋 Проверить статус

```bash
# Посмотреть запущенные контейнеры
docker ps

# Посмотреть логи
docker-compose logs -f

# Посмотреть последние изменения
git log --oneline -5
```

---

## 🆘 Если что-то пошло не так

### Контейнер не запускается

```bash
# Полная очистка и пересборка
docker-compose down
docker system prune -f
docker-compose build --no-cache
docker-compose up -d
```

### Порт 3000 занят

```bash
# Найти и остановить процесс
kill -9 $(lsof -ti :3000)

# Затем запустить Docker
docker-compose up -d
```

### Ошибки git pull

```bash
# Сбросить локальные изменения (ОСТОРОЖНО!)
git reset --hard origin/claude/read-project-file-011CUxhFPATd3uJNSpJNTcGw
```

---

## 💡 Рекомендация

**Используйте двойной клик на `update-and-restart.command`** - это самый простой способ!

Файл можно даже добавить в Dock для быстрого доступа:
1. Перетащите `update-and-restart.command` в Dock
2. При обновлениях - просто кликните на иконку в Dock

---

**Всё готово!** 🎉
