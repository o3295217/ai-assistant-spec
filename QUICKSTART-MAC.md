# 🚀 Быстрый старт на macOS

Пошаговое руководство для запуска **Personal AI Effectiveness Assistant** на Mac.

---

## ⚡ Быстрый запуск (если всё уже настроено)

```bash
./start.sh
```

Откройте браузер: http://localhost:3000

---

## 🔧 Первая установка

### 1. Клонируйте репозиторий (если ещё не сделали)

```bash
git clone <url-репозитория>
cd ai-assistant-spec
```

### 2. Сделайте скрипты исполняемыми

```bash
chmod +x start.sh setup-mac.sh stop.sh
```

### 3. Запустите полную установку

```bash
./setup-mac.sh
```

Этот скрипт автоматически:
- ✅ Проверит Node.js (предложит установить если нужно)
- ✅ Установит все зависимости
- ✅ Создаст базу данных SQLite
- ✅ Создаст файл конфигурации .env.local
- ✅ Подготовит проект к запуску

### 4. Настройте API ключ Anthropic

**Важно для работы ИИ-оценки!**

1. Зарегистрируйтесь на https://console.anthropic.com/
2. Создайте API ключ
3. Откройте файл `.env.local`:
   ```bash
   open .env.local
   # или
   nano .env.local
   ```
4. Замените строку:
   ```
   ANTHROPIC_API_KEY="sk-ant-your-api-key-here"
   ```
   на:
   ```
   ANTHROPIC_API_KEY="sk-ant-ваш-настоящий-ключ"
   ```
5. Сохраните файл

### 5. Запустите приложение

```bash
./start.sh
```

---

## 🎮 Управление приложением

### Запуск
```bash
./start.sh
```

### Остановка
```bash
# Нажмите Ctrl+C в терминале
# или используйте скрипт:
./stop.sh
```

### Перезапуск
```bash
./stop.sh && ./start.sh
```

---

## 📍 Страницы приложения

После запуска откройте в браузере:

| Страница | URL | Описание |
|----------|-----|----------|
| **Главная** | http://localhost:3000 | Обзор проекта |
| **Цели** | http://localhost:3000/goals | Управление целями |
| **Сегодня** | http://localhost:3000/daily | Планирование дня |
| **История** | http://localhost:3000/history | Календарь с оценками |
| **Аналитика** | http://localhost:3000/analytics | Графики и статистика |
| **Отчеты** | http://localhost:3000/reports | Недельные отчеты |
| **Задачи** | http://localhost:3000/tasks | Незакрытые задачи |

---

## 🔍 Решение проблем

### Порт 3000 занят

```bash
# Найти процесс на порту 3000
lsof -ti:3000

# Убить процесс
kill -9 $(lsof -ti:3000)

# Или используйте stop.sh
./stop.sh
```

### Ошибка "command not found: node"

Node.js не установлен. Установите:

**Через Homebrew:**
```bash
brew install node
```

**Или скачайте с:**
https://nodejs.org/ (рекомендуется LTS версия)

### Ошибка "Cannot find module"

Переустановите зависимости:
```bash
rm -rf node_modules package-lock.json
npm install
```

### База данных не создается

Пересоздайте вручную:
```bash
rm -f prisma/dev.db
node setup-db.js
```

### Приложение не запускается

1. Проверьте версию Node.js (должна быть 18+):
   ```bash
   node -v
   ```

2. Очистите кеш Next.js:
   ```bash
   rm -rf .next
   ```

3. Перезапустите:
   ```bash
   ./start.sh
   ```

---

## 📦 Системные требования

- **macOS:** 10.15 или новее
- **Node.js:** v18.0.0 или новее
- **npm:** v9.0.0 или новее
- **Свободное место:** ~500 MB (с зависимостями)
- **Интернет:** Нужен для установки и работы Claude API

---

## 🛠️ Дополнительные команды

### Обновить зависимости
```bash
npm update
```

### Просмотреть логи
```bash
# Next.js логи
cat .next/dev/logs/next-development.log

# Логи приложения (если включено логирование)
tail -f logs/app.log
```

### Очистить все временные файлы
```bash
rm -rf .next node_modules logs
npm install
```

### Создать резервную копию базы данных
```bash
cp prisma/dev.db prisma/dev.db.backup
```

---

## 🚀 Рабочий процесс

### Ежедневное использование

**Утром:**
1. Запустите приложение: `./start.sh`
2. Откройте http://localhost:3000/daily
3. Создайте план на день
4. Сохраните

**Вечером:**
1. Вернитесь на http://localhost:3000/daily
2. Заполните факт выполнения
3. Нажмите "Получить оценку от ИИ"
4. Проанализируйте результаты и alignment
5. Прочитайте рекомендации

**Еженедельно:**
- Проверьте отчет на http://localhost:3000/reports
- Обновите цели на http://localhost:3000/goals
- Проверьте аналитику на http://localhost:3000/analytics

---

## 💡 Советы для macOS

### Автозапуск при входе в систему (опционально)

Создайте файл `~/Library/LaunchAgents/com.ai-assistant.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.ai-assistant</string>
    <key>ProgramArguments</key>
    <array>
        <string>/путь/к/проекту/start.sh</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
</dict>
</plist>
```

### Использование VS Code

```bash
# Открыть проект в VS Code
code .

# Редактировать .env.local
code .env.local
```

### Горячие клавиши в терминале

- `Ctrl+C` - Остановить сервер
- `Cmd+K` - Очистить терминал
- `Cmd+T` - Новая вкладка терминала

---

## 📚 Дополнительная документация

- **README.md** - Полное руководство по проекту
- **LOGGING.md** - Система логирования
- **ai-assistant-spec.md** - Техническое задание

---

## ✅ Контрольный список первого запуска

- [ ] Node.js установлен (v18+)
- [ ] Выполнен `./setup-mac.sh`
- [ ] API ключ Anthropic добавлен в `.env.local`
- [ ] База данных создана (`prisma/dev.db` существует)
- [ ] Приложение запускается (`./start.sh`)
- [ ] Браузер открывает http://localhost:3000
- [ ] Создана первая мечта на 5 лет
- [ ] Установлены цели на год
- [ ] Создан первый план дня

---

**Всё готово! Начните управлять своей эффективностью! 🎯**
